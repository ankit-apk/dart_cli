name: dart_app
description: A command-line application which creates flutter app with specified dependencies before hand.
version: 1.0.0
repository: https://github.com/ankit-apk/dart_cli

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  process_run: ^0.13.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
