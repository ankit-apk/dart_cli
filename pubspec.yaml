name: dart_app_cli
description: Creates flutter app with specified dependencies before hand.
version: 1.0.2
repository: https://github.com/ankit-apk/dart_cli

environment:
  sdk: ^3.0.6

# Add regular dependencies here.
dependencies:
  args: ^2.4.2
  process_run: ^0.13.1
  # path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0

executables:
  dart_app_cli: dart_app_cli